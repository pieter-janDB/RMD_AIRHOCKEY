<li data-socketid={{socketId}}>
  {{#if self}}
    <span class ="ownName">{{nickname}}</span>
  {{else}}
    <span class="nickname">{{nickname}}</span>
    <button class="connect">connect</button>
  {{/if}}
</li>
